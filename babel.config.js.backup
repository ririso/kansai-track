module.exports = {
  // Jest環境でのみBabelを使用（開発時はSWCを利用）
  env: {
    test: {
      presets: [
        ["@babel/preset-env", { targets: { node: "current" } }],
        "@babel/preset-typescript", // TS + TSX
        ["@babel/preset-react", { runtime: "automatic" }],
      ],
    },
  },
};